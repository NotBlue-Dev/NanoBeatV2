/*
###############################
wall class:
 - When cut block -> color (depends on saber/color/points)
###############################
*/

class cut {
    constructor(manage,options,panel) {
        this.manage = manage
        this.options = options
        this.panel = panel
    }

    handle(httpData) {

    }
}

module.exports = cut